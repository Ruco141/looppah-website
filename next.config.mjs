/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    images: {
        qualities: [75, 90],
    },
    allowedDevOrigins: ['192.168.100.103'],
};

export default nextConfig;
